{% extends 'base.html.twig' %}

{% block title %}Repa
{% endblock %}

{% block body %}
	<h1>Repa</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ repa.id }}</td>
			</tr>
			<tr>
				<th>Type</th>
				<td>{{ repa.type }}</td>
			</tr>

		</tbody>
	</table>


	<div class="btn-group ml-3 float-right mb-2" role="groupe">
		<a href="{{ path('repa_index') }}">
			<button class="btn btn-outline-secondary">Back</button>
		</a>
		<div class="ml-2 mr-2">
			{{ include('repa/_delete_form.html.twig') }}
		</div>
		<a href="{{ path('repa_edit', {'id': repa.id}) }}">
			<button class="btn btn-outline-secondary">edit</button>
		{% endblock %}
