{% extends 'base.html.twig' %}

{% block title %}Repa{% endblock %}

{% block body %}
    <h1>Repa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ repa.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ repa.type }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ repa.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('repa_index') }}">back to list</a>

    <a href="{{ path('repa_edit', {'id': repa.id}) }}">edit</a>

    {{ include('repa/_delete_form.html.twig') }}
{% endblock %}
