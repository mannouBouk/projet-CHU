{% extends 'base.html.twig' %}

{% block title %}Nomonclature
{% endblock %}

{% block body %}
	<h3>Nomonclature</h3>

	<table class="table">
		<tbody>

			<tr>
				<th>repa</th>
				<td>{{ nomonclature.repa }}</td>
			</tr>
			<tr>
				<th>Regime</th>
				<td>{{ nomonclature.regime }}</td>
			</tr>
			<tr>
				<th>Prix</th>
				<td>{{ nomonclature.prix }}</td>
			</tr>


		</tbody>
	</table>
	<div class="btn-group ml-3 float-right mb-2" role="groupe">
		<a href="{{ path('app_nomonclature_index') }}">
			<button class="btn btn-outline-secondary">Back</button>
		</a>
		<div class="ml-2 mr-2">

			{{ include('nomonclature/_delete_form.html.twig') }}
		</div>
		<a href="{{ path('app_nomonclature_edit', {'id': nomonclature.id}) }}">
			<button class="btn btn-outline-secondary">edit</button>
		</a>

	{% endblock %}
